
<div class="row">

  <!-- Line Chart - Single Series -->
  <div class="col-xs-6">


  </div>

</div>

<h1><%= @university_name %> 의 학점 분포 현황</h1>
<h2>
    
    <%= @query %>
    <%= @chart_data %>
    <%= pie_chart @chart_data%>
    <%= column_chart @chat_data%>
    
</h2>

<h1>학교 지역</h1>
<% @univ.each do |p|%>
    <%=p.id %> /<%= p.name %> / <%= p.location %> / <%= p.is_4_5 %> / <%= p.is_public %> / <%= p.total_student_1 %> / <%= p.aplus_students_1 %> / <%= p.aplus_ratio_1 %> / <%= p.aplus_students_2 %> / <%= p.aplus_ratio_2 %><br>

<% end %>
