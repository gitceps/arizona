<div class="bg"></div>
<div class="row">
  <div align=center> 
    <h2><span>이메일로 비밀번호 변경하기</span></h2>

    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= devise_error_messages! %>

      <div class="field">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true %>
      </div>
      <br>
      <div class="actions">
        <%= f.submit "이메일 보내기" %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
